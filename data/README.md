# Data

Put evaluation datasets here.

Expected JSONL schema:
```json
{"id":"1","question":"...","context":"...","ground_truth":"..."}
```

`sample_questions.jsonl` is only a smoke-test dataset for local verification. Replace it with your real 100+ question evaluation set for the final experiments.
