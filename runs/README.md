# Runs

Generated run logs and summaries go here.

For the assignment submission, keep the final `runs/<run_id>.jsonl` and
`runs/<run_id>.summary.json` files in version control so the reported results
are reproducible from the repository itself.
